# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
Desktop.ini

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environment (created by the installer)
.venv/
venv/
ENV/

# IDE / assistant scratch
.vscode/
.idea/
.claude/

# Test/Coverage
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Generated macOS launcher artefacts (built by tools/create_macos_app.sh)
MMForge.app/
tools/mmforge.icns

# Analysis output written by the app.
# The GUI defaults to ~/MMForge_output, but users can point it anywhere -
# including here - so keep these out of the repository.
calibration_output/
processing_output/
decomposition_output/

# Stray result files dropped in the project root (exports, saved sessions).
# Only the repository root is matched, so data/assets/*.txt and the bundled
# tutorial files are unaffected.
/*.csv
/*.npz
